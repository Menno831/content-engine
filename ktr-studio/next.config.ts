import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Losse app — staat in /ktr-studio binnen de content-engine repo.
  // Later te verplaatsen naar een eigen repo + deploy op app.ktrstudio.nl.
};

export default nextConfig;
